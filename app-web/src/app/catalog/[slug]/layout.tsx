export const metadata = {
  title: 'Catálogo de Produtos',
  description: 'Confira nosso catálogo de produtos e faça seu pedido',
};

export default function CatalogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
